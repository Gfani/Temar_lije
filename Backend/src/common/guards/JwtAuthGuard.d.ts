import { CanActivate } from '@nestjs/common';

/**
 * Type declaration for the CommonJS JwtAuthGuard (JwtAuthGuard.js) so
 * TypeScript files can reference it in @UseGuards(). At runtime the
 * compiled .js is loaded; this file only exists for type resolution.
 */
export declare class JwtAuthGuard implements CanActivate {}